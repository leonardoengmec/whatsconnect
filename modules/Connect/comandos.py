from app.mac import mac, signals
import os
'''
@signals.command_received.connect
def handle(message):
    if message.command == "atualizarFoto":
        mac.set_profile_picture(os.path.abspath("static/logo.jpg"))
''' 
